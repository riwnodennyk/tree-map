{
  "name": "building-age-map",
  "version": "1.0.0",
  "description": "Map of building construction years",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.37.1",
    "@types/leaflet": "^1.9.12",
    "@types/osmtogeojson": "^2.2.34",
    "leaflet": "^1.9.4",
    "vite": "^6.0.0",
    "wrangler": "^4.92.0"
  },
  "dependencies": {
    "osmtogeojson": "^3.0.0-beta.5"
  }
}
